[tox]
requires =
    tox>=4
    tox-uv
env_list =
    py314
    lint

[testenv]
runner = uv-venv-lock-runner
description = run tests
allowlist_externals = uv
deps =
    pytest
commands =
    pytest tests -v

[testenv:lint]
runner = uv-venv-lock-runner
description = run ruff
allowlist_externals =
    uv
    ruff
deps =
    ruff
commands =
    ruff check django_gc
    ruff format --check django_gc
ignore_errors = true
